{% set name = "caf.space" %}
{% set version = "1.2.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/caf.space-{{ version }}.tar.gz
  sha256: 0c5844900d88a1709a248a34377737ba286ed1c53b80aa072fe9951ac75b048c

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.9
    - setuptools_scm
    - versioneer-518
    - pip
  run:
    - python >=3.9
    - caf.toolkit >=0.0.7
    - geopandas >=0.9
    - fiona >=1.8
    - shapely >=1.8
    - numpy >=1.21
    - pandas >=1.3
    - pydantic >=1.10.2
    - openpyxl >=3.0

test:
  imports:
    - caf.space
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/Transport-for-the-North/caf.space
  summary: Easily generate translations between transport zoning systems
  dev_url: https://github.com/Transport-for-the-North/caf.space
  license: GPL-3.0-only
  license_file: LICENSE

extra:
  recipe-maintainers:
    - isaac-tfn
